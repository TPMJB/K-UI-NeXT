diff -ruN a/.github/workflows/diagnostic.yml b/.github/workflows/diagnostic.yml
--- a/.github/workflows/diagnostic.yml	2026-09-19 04:25:52.115195085 +0000
+++ b/.github/workflows/diagnostic.yml	2026-09-19 04:25:52.127837377 +0000
@@ -26,6 +26,11 @@
       - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
       - name: Install filesystem test utilities
         run: sudo apt-get update && sudo apt-get install -y dosfstools exfatprogs mtools
+      - name: Restore pinned FatFs downloads
+        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
+        with:
+          path: .deps/downloads
+          key: fatfs-downloads-v1-${{ hashFiles('dependencies.json') }}
       - name: Fetch verified FatFs sources
         run: python3 tools/fetch_deps.py --fatfs-only
       - name: Run portable and real filesystem checks
@@ -43,6 +48,11 @@
           sudo apt-get install -y build-essential git curl wget patch python3 \
             bison flex texinfo gettext libgmp-dev libmpfr-dev libmpc-dev libisl-dev \
             meson ninja-build pkg-config libisofs-dev libpng-dev libjpeg-dev
+      - name: Restore pinned FatFs downloads
+        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
+        with:
+          path: .deps/downloads
+          key: fatfs-downloads-v1-${{ hashFiles('dependencies.json') }}
       - name: Restore toolchain and source downloads
         uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
         with:
diff -ruN a/tools/fetch_deps.py b/tools/fetch_deps.py
--- a/tools/fetch_deps.py	2026-09-19 04:25:52.113050919 +0000
+++ b/tools/fetch_deps.py	2026-09-19 04:25:52.127655695 +0000
@@ -7,6 +7,8 @@
 from pathlib import Path
 import shutil
 import subprocess
+import time
+import urllib.error
 import urllib.request
 import zipfile
 
@@ -19,6 +21,31 @@
     subprocess.run(args, cwd=cwd, check=True)
 
 
+def download(url, attempts=5):
+    """Fetch one pinned file, retrying transient network failures.
+
+    elm-chan.org is a single small host and regularly times out from CI
+    runners. A single urlopen there fails the whole build for a reason that
+    has nothing to do with the code, so retry with a widening backoff and a
+    longer timeout. The caller still checks the SHA-256, so a retry can never
+    weaken the pin: a truncated or substituted body is rejected either way.
+    """
+    last = None
+    for attempt in range(1, attempts + 1):
+        try:
+            request = urllib.request.Request(url, headers={"User-Agent": "K-UI-NeXT/deps"})
+            with urllib.request.urlopen(request, timeout=180) as response:
+                return response.read()
+        except (urllib.error.URLError, TimeoutError, OSError) as error:
+            last = error
+            if attempt == attempts:
+                break
+            delay = 2 ** attempt
+            print(f"  {url}: {error}; retry {attempt}/{attempts - 1} in {delay}s", flush=True)
+            time.sleep(delay)
+    raise SystemExit(f"Could not download {url} after {attempts} attempts: {last}")
+
+
 def fetch_git(name):
     dest = DEPS / name
     spec = LOCK[name]
@@ -43,8 +70,8 @@
     for spec in LOCK["fatfs"]:
         target = cache / spec["url"].rsplit("/", 1)[-1]
         if not target.exists():
-            with urllib.request.urlopen(spec["url"], timeout=60) as response:
-                data = response.read()
+            print(f"Downloading {spec['url']}", flush=True)
+            data = download(spec["url"])
             if hashlib.sha256(data).hexdigest() != spec["sha256"]:
                 raise SystemExit(f"Download checksum mismatch: {target.name}")
             target.write_bytes(data)
